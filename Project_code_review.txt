CRITICAL ISSUES
1. Direct State Mutations (React Anti-Pattern)
Location	Issue
MCVP/InteractiveInput.jsx:248	graphData.links.pop() mutates state array directly without setGraphData. React will not re-render, causing UI desync.
CombinatorialGame/ManualInput.jsx:464	node.player = node.player === 1 ? 2 : 1 mutates the node object inside setGraph, then spreads a shallow copy. React may not detect the change.
MCVP/TreeRenderCanvas.jsx:94-145	useMemo callback mutates original tree nodes (adds id, neighbors, links properties, clears evaluationResult). Side effects in useMemo violate React's purity contract.
2. Crash-Causing Bugs
Location	Issue
CombinatorialGame/ComputeWinner.js:21	Error return { hasWinningStrategy: false, error: "..." } is missing steps array. StepByStepGame.jsx:29 accesses result.steps, causing a crash.
CombinatorialGame/StepByStepGame.jsx:70,133	graph.positions[step.id].player accessed without null check. If step references a non-existent position, throws TypeError.
Conversions/ConversionCombinatorialGame.js:22-23	steps[steps.length - 1].graph crashes when steps is empty (returns undefined.graph).
MCVPtoCombinatorialGameConverter.jsx:188	"Next" button enabled when steps is empty (0 === -1 is false).
3. Parser Bugs (User-Facing)
Location	Issue
MCVP/Parser.js:204	Parser regex (\d) matches only single-digit variable values. Input x1[10] silently coerces to value 1 instead of 10.
Grammar/GrammarParser.js:8-9	Blank lines in input cause "Neplatny format pravidla" error. Parser should skip empty lines.
Grammar/GrammarParser.js:9	Multiple -> arrows (e.g., A -> B -> C) silently drops the third part.
HIGH-SEVERITY ISSUES
4. Duplicate Computation
Location	Issue
StepByStepGame.jsx:21	Calls computeWinner(graph) again despite parent CombinatorialGame.jsx:34 already computing it. This is an expensive algorithm running twice on the same data. Steps should be passed as a prop.
GrammarStepEvaluator.js vs GrammarEvaluator.js	Entire productive-set algorithm is copy-pasted between these two files (~80 lines). If one is fixed, the other diverges.
5. Performance — Re-render Triggers
Location	Issue
DisplayGraph.jsx:9	Default param optimalMoves = new Set() creates a new object every render, invalidating the data useMemo on every parent re-render. Fix: use a module-level const EMPTY_SET = new Set().
StepByStepTree.jsx:59	steps.slice(0, currentStep + 1) creates a new array every render, invalidating downstream memos. Should be wrapped in useMemo.
CombinatorialGame.jsx:29-61	useMemo depends on chosenOpt and selectedStartingPlayer, causing computeWinner to re-run when input tab changes (even though the graph hasn't changed).
6. No Error Boundary
App.jsx has no React Error Boundary. Any render-time exception (malformed import data, canvas error, etc.) crashes the entire app with a white screen.

7. React StrictMode Disabled
main.jsx:7-9 — StrictMode is commented out, hiding double-render and side-effect bugs during development.

MEDIUM-SEVERITY ISSUES
8. Major Code Duplication (DRY Violations)
Pattern	Locations	Duplicated Lines
Flat-graph-to-tree conversion	Serialization.js, PreparedSetsInput.jsx, InteractiveInput.jsx	~70 lines × 3
PreparedSetsInput data loading	MCVP, CombinatorialGame, Grammar modules	~25 lines × 3
Step navigation (state, effects, UI)	MCVPtoGrammarConverter.jsx, MCVPtoCombinatorialGameConverter.jsx	~50 lines × 2
ResizeObserver setup	TreeRenderCanvas.jsx, InteractiveInput.jsx, DisplayGraph.jsx, DerivationTreeVisual.jsx	~20 lines × 4
Operator display name mapping (A/O/AND/OR/∧/∨)	TreeRenderCanvas.jsx, StepByStepTree.jsx, InteractiveInput.jsx, EvaluateCircuit.js	~6 lines × 4
typeof link.source === 'object' ? link.source.id : link.source	CombinatorialGame/ManualInput.jsx alone	10+ occurrences
Modal.jsx vs ConversionModal.jsx	Both files	Nearly identical close animation logic
Number input clamping	MCVP and CombinatorialGame GenerateInput.jsx	~5 lines × 4
9. Algorithm Inefficiencies
Location	Issue	Complexity
MCVP/Parser.js:32	s.slice(pos) creates a substring for every token pattern on every position.	O(n² × k)
GrammarEvaluator.js:71	JSON.stringify for deep equality in witness deduplication.	O(witnesses × symbols) per call
GrammarEvaluator.js:42	terminals.includes(sym) is O(n) in inner loop. Convert to Set.	O(rules × symbols × terminals)
CombinatorialGame/Generator.js:37-52	O(N²) nested loop for edge generation + Array.includes for edge dedup.	O(N² × K)
Multiple files	queue.shift() in BFS loops (ComputeWinner.js:58, GrammarEvaluator.js:61, GrammarGenerator.js:98, Serialization.js:90). Array.shift() is O(n).	O(n²) total for BFS
10. NaN Propagation
Location	Issue
MCVP/GenerateInput.jsx:33-37	Number("abc") → NaN. Neither NaN > 750 nor NaN < 1 is true, so setNumGates(NaN) is called. Same on lines 49-53.
CombinatorialGame/GenerateInput.jsx:56-60	Same issue with Number(e.target.value) clamping.
11. Global Mutable State
MCVP/NodeClass.js:8,40-42 — globalIdCounter is a module-level mutable variable. Both Parser.js and Generator.js call Node.resetIdCounter(). In concurrent scenarios or React's concurrent mode, this creates ID collision risk.

12. Direct DOM Manipulation
Navigation.jsx:13-62 — Uses document.querySelector, document.createElement, classList.add/remove for offcanvas menu instead of React state. This bypasses React's reconciliation and is fragile.

13. Memory Leaks (Uncleaned Timeouts)
Location	Issue
Navigation.jsx:71-75	setTimeout in closeMobileMenu not tracked.
Modal.jsx:9-11	setTimeout in handleClose not cleaned on unmount.
ConversionModal.jsx:9-11	Same as Modal.
MCVPtoCombinatorialGameConverter.jsx:90,97	setTimeout in event handlers not tracked.
14. Inconsistent Operator Representation
AND gates appear as 'A', 'AND', or '∧'; OR gates as 'O', 'OR', or '∨' across the codebase. Every rendering/evaluation point must check all variants. A single canonical form + normalization function would eliminate this.

15. Inconsistent Node ID Types
Throughout CombinatorialGame, node IDs are sometimes strings, sometimes numbers. ManualInput.jsx uses String(pos.id), PreparedSetsInput.jsx uses String(edge.source), while other places use strict === comparison. This creates subtle equality bugs.

LOW-SEVERITY ISSUES
16. Accessibility Deficiencies
Issue	Locations
Canvas elements have no text alternative	NodeVisual.jsx, TreeRenderCanvas.jsx, InteractiveInput.jsx, all ForceGraph2D usages
Icon-only buttons without aria-label	StepByStepTree.jsx:90-101, StepByStepGame.jsx:111-121, Grammar.jsx:156-162
InfoButton.jsx is mouse-hover only (no keyboard/touch)	InfoButton.jsx:11-12
Modals have no focus trap, no role="dialog", no Escape key handler	Modal.jsx, ConversionModal.jsx
Form inputs lack htmlFor/id association	All ManualInput, GenerateInput, PreparedSetsInput across modules
Color alone used to distinguish game elements	CombinatorialGame/ManualInput.jsx paint functions
Advanced toggle missing aria-expanded	Grammar/GenerateInput.jsx:127-132
17. PropTypes Issues
Issue	Locations
PropTypes.object without .shape()	MCVP.jsx:191, TreeRenderCanvas.jsx:396-397, StepByStepTree.jsx:126, CombinatorialGame.jsx:238, MCVPtoCombinatorialGameConverter.jsx:208
Missing .isRequired on critical props	StepByStepTree.jsx:117 (tree), InteractiveInput.jsx:579 (onTreeUpdate)
Dead/unused prop declarations	CombinatorialGame.jsx:238 (onNavigate never used), Grammar.jsx:211 (onNavigate never used)
18. Minor Code Issues
Location	Issue
GenerateInput.jsx:7 (CombinatorialGame)	Typo: edgePropability should be edgeProbability
Grammar/ManualInput.jsx:7	setinputText violates camelCase convention
Multiple PreparedSetsInput files	parseInt() without radix parameter
StepByStepGame.jsx:163	Pointless ternary: activeStep.hasWinningStrategy ? 'fw-bold' : 'fw-bold'
Grammar/ManualInput.jsx:24-27	<div> inside <h3> — invalid HTML
Grammar/GrammarGenerator.js:203-205	generateTerminals() produces duplicates when count > 26
NodeVisual.jsx:28-73	Node class defined inside component body, recreated every render
EvaluateCircuit.js:49	Dead code: childValues.some(v => v === null) is always false since null values are pre-filtered
19. CSS Issues
Location	Issue
style.css:615,640	Duplicate .form-switch .form-check-input selector with conflicting width values (46px vs 44px)
style.css:551-554	margin-bottom: 10px immediately overridden by margin: auto then margin-bottom: 30px
style.css throughout	30+ uses of !important to fight Bootstrap specificity
ARCHITECTURAL RECOMMENDATIONS
Extract shared utilities: A useContainerDimensions hook, useStepNavigation hook, loadDataSets utility, and getOperatorDisplayName helper would eliminate hundreds of duplicated lines.

Normalize data models: Adopt a single canonical form for operator values and node ID types across the codebase.

Add Error Boundaries: Wrap page components in Error Boundaries to prevent full-app crashes.

Re-enable StrictMode: Uncomment StrictMode in main.jsx to catch side-effect bugs.

Consider TypeScript: The project uses JSDoc + PropTypes for type safety, but TypeScript would catch many of the identified bugs at compile time (type mismatches, missing properties, nullable access).

Convert BFS queues: Replace Array.shift() with index-based dequeuing across all BFS/work-list algorithms.

Centralize validation: Move Grammar and graph validation logic into constructors instead of duplicating it across import handlers and PreparedSetsInput components.

Total issues found: ~85 (3 critical, 7 high, 15 medium, ~60 low-severity)

Completed: Compile final report with findings (5/5)

The report above covers every source file in the project. The most impactful areas to address first would be:

The three crash-causing bugs (missing steps in error return, unguarded .graph access on empty array, null position access)
Direct state mutations (3 locations) — these cause silent UI desyncs
The duplicate computeWinner call — runs an expensive algorithm twice unnecessarily
The duplicated evaluator algorithm between GrammarEvaluator.js and GrammarStepEvaluator.js — a maintenance risk