---- MCVP ----
- Fix - when user clicks on the explain button, it recalculates the derived word 
- Fix - placement of the explain button should be like in other problems under all page content
- Fix - in MCVP to CG disable the button to end or to beginnig if the user is at the end or begining of the conversion

- Optimize - the nodes are still overlaping, when there are more children - find a way to idealy place all the nodes at the begining
- Optimize (interactive) - make highlight style of nodes and edges same as TreeRenderCanvas
- Optimize (interactive) - make the arrows bigger
- Optimize (generated) - advanced options can be added (max/min num of children ) 
- Optimize - make just the variables smaller to fit the nodes, operations can stay the same

---- CG ----
- Fix the evaluating alg, its enough to have one winnig strategy to evaluate that the player I have winnig strategy
- Fix display of the optimal moves (the whole path should be highlighted)
- Fix - when cycle, in explain modal show the cycle and maybe the steps
- Optimize - dont reheat the fyzics when manipulating with graph
- Optimize - make the starting lenght of edges longer

---- Grammar ----
- Check if epsilon counts as empty grammar 
- Optimize - adjust the node radius for the longer words
- Optimize - (manual) make the text area as big as the content automaticly 

---- Global ----
- Add autoscroll after selecting set or genererating and displaying some data 
- Add fixed positions for nodes when exporting some graph + position the graph when provided with fixed positions

- Bonus - implemnt dynamic size of display component, user can resize the coimponent manually (by pulling the edge or + / - buttons)

-udelat novou vetev s omezenout fyzikou

===================================================Uprava pisemne casti=========================================================================================================

1. Odborné a implementační připomínky
    [DONE]
    MCVP (Circuit Value Problem): Musíš důsledně rozlišovat mezi stromem a DAGem (orientovaným acyklickým grafem). 
    P-úplnost platí pro DAGy; u stromů je problém jednodušší. V textu i v programu bys měl pracovat s DAGem.

    [DONE]
    Kombinatorické hry: Vedoucí doporučuje zobecnit implementaci z acyklického grafu na obecný orientovaný graf. 
    K vyhodnocení vítězné strategie navrhuje použít algoritmus s frontou (podobný prohledávání do šířky, ale od koncových uzlů), aby se předešlo problémům s nekonečnými cykly.

    Gramatiky: Navrhuje upravit pravidla pro identifikaci neterminálů. Místo "všechna písmena velká" by stačilo, aby první písmeno bylo velké (což by umožnilo názvy jako A1 nebo A_10).

2. Struktura a forma textu (Zásadní) [DONE]
    Aktuální struktura je podle něj nevyhovující. Navrhuje klasické akademické dělení:

    Teoretický základ: Formální definice (PTIME, P-úplnost, logaritmická redukce) a teoretický popis problémů.

    Analýza a návrh: Volba technologií, knihoven a softwarové diagramy (např. třídní diagram, stavový diagram).

    Implementace: Konkrétní popis kódu, reprezentace dat v programu a samotné algoritmy převodů.

3. Grafické podklady
    [DONE]
    Smazat: Zbytečné obrázky (např. obr. 3.4), které nenesou informaci.

    Přidat: Ilustrace převodů (jak vypadá konkrétní MCVP a jak se změní na hru/gramatiku).

    Doplnit: Celkové screenshoty aplikace pro představu o UI.